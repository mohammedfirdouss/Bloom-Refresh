# Environment variables for Bloom Refresh Frontend

# Example API endpoint (replace with actual backend URL when available)
NEXT_PUBLIC_API_BASE_URL="http://localhost:5000/api"

# Add other environment variables as needed

